import "Standard";
import "Globals";

Globals["score"] = 0;

this.Behaviour.Update = () => {
	//This script must be called from ToyBehaviour, otherwise "Globals" won't be saved into ToyBehaviour's environment
	this.TextMesh.SetText("Score: " + ToString( Globals["score"] ));
};